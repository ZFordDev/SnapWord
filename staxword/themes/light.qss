/* StaxWord — Light Theme (Google Docs-style) */

/* ── Global defaults ─────────────────────────────────────── */
QWidget {
    background-color: #ffffff;
    color: #202124;
    font-family: 'Segoe UI', 'SF Pro Text', 'Ubuntu', sans-serif;
    font-size: 13px;
}

/* ── Menu bar ────────────────────────────────────────────── */
QMenuBar {
    background-color: #f8f9fa;
    border-bottom: 1px solid #dadce0;
    padding: 4px 8px;
}

QMenuBar::item {
    background: transparent;
    padding: 4px 10px;
    color: #5f6368;
    border-radius: 4px;
}

QMenuBar::item:selected {
    background-color: #e8eaed;
    color: #202124;
}

/* ── Dropdown menus ──────────────────────────────────────── */
QMenu {
    background-color: #ffffff;
    border: 1px solid #dadce0;
    border-radius: 8px;
    padding: 4px 0px;
}

QMenu::item {
    padding: 6px 20px 6px 16px;
    color: #3c4043;
}

QMenu::item:selected {
    background-color: #e8f0fe;
    color: #1967d2;
}

/* ── Toolbar ─────────────────────────────────────────────── */
QToolBar {
    background-color: #f8f9fa;
    border-bottom: 1px solid #dadce0;
    padding: 2px 8px;
    spacing: 2px;
}

QToolBar QToolButton {
    background: transparent;
    border: 1px solid transparent;
    border-radius: 4px;
    padding: 4px 8px;
    color: #5f6368;
    font-size: 13px;
    font-weight: 600;
    min-width: 28px;
    min-height: 28px;
}

QToolBar QToolButton:hover {
    background-color: #e8eaed;
    border-color: #dadce0;
}

QToolBar QToolButton:checked {
    background-color: #e8f0fe;
    border-color: #1a73e8;
    color: #1a73e8;
}

QToolBar QToolButton:pressed {
    background-color: #d2e3fc;
}

/* Bold button */
QToolBar QToolButton#BoldBtn {
    font-weight: 800;
    font-size: 14px;
}

/* Italic button */
QToolBar QToolButton#ItalicBtn {
    font-style: italic;
    font-size: 14px;
}

/* Underline button */
QToolBar QToolButton#UnderlineBtn {
    font-size: 14px;
    text-decoration: underline;
}

/* Strikethrough button */
QToolBar QToolButton#StrikethroughBtn {
    font-size: 14px;
    text-decoration: line-through;
}

/* Font combo boxes */
QToolBar QFontComboBox {
    background-color: #ffffff;
    border: 1px solid #dadce0;
    border-radius: 4px;
    padding: 2px 8px;
    color: #3c4043;
    font-size: 12px;
    min-height: 26px;
}

QToolBar QFontComboBox:hover {
    border-color: #1a73e8;
}

QToolBar QFontComboBox::drop-down {
    border: none;
    width: 20px;
}

QToolBar QComboBox {
    background-color: #ffffff;
    border: 1px solid #dadce0;
    border-radius: 4px;
    padding: 2px 8px;
    color: #3c4043;
    font-size: 12px;
    min-height: 26px;
}

QToolBar QComboBox:hover {
    border-color: #1a73e8;
}

QToolBar QComboBox::drop-down {
    border: none;
    width: 20px;
}

/* ── Sidebar (file tree) ─────────────────────────────────── */
QTreeView#FileTreeList {
    background-color: #f8f9fa;
    border: none;
    border-right: 1px solid #dadce0;
    padding: 12px;
    font-size: 13px;
}

QTreeView::item {
    padding: 5px 8px;
    border-radius: 4px;
    color: #3c4043;
    margin: 1px 4px;
}

QTreeView::item:hover {
    background-color: #e8eaed;
}

QTreeView::item:selected {
    background-color: #e8f0fe;
    color: #1967d2;
}

/* ── Splitter handles ────────────────────────────────────── */
QSplitter#AppContainer::handle {
    background-color: #dadce0;
    width: 1px;
}

/* ── Page workspace (gray background) ────────────────────── */
QWidget#PageWorkspace {
    background-color: #f1f3f4;
    border: none;
}

/* ── Page wrapper (centers the white page) ───────────────── */
QWidget#PageWrapper {
    background: transparent;
}

/* ── Page container (white page with shadow) ─────────────── */
QWidget#PageContainer {
    background-color: #ffffff;
    border: 1px solid #dadce0;
    border-radius: 2px;
}

/* ── Editor (rich text) ──────────────────────────────────── */
QTextEdit#RichTextEdit {
    background-color: #ffffff;
    color: #202124;
    padding: 72px 96px;
    border: none;
    selection-background-color: #d3e3fd;
    font-family: 'Arial', 'Calibri', sans-serif;
    font-size: 11pt;
    line-height: 1.5;
}

/* ── Footer ──────────────────────────────────────────────── */
QWidget#AppFooter {
    background-color: #f8f9fa;
    border-top: 1px solid #dadce0;
    max-height: 28px;
}

QWidget#AppFooter QLabel {
    color: #5f6368;
    font-size: 11px;
}
